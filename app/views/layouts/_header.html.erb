<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
            <%= link_to(image_tag("/assets/weinevents-logo-3.png", :title => 'Weinevents', :alt => "Weinevents logo", :size => "250x400", :class => "brand"), "/") %>

                <div class="nav-collapse">
                    <% if user_signed_in? && current_user.role?(:super_admin) %>
                        <ul class="nav">
                            <li>    
                                <%= link_to t(:nav_cities),  sitecities_path %>
                            </li>
                            <li>    
                                <%= link_to t(:nav_about),  about_path %>
                            </li>
                        </ul>
                    <% end %>
                    <ul class="nav pull-right">
                      <% if user_signed_in? %>
                        <li><%= link_to t(:new_event), new_event_path %></li>
                        <li><%= link_to t(:edit_profile), edit_user_registration_path %></li>
                        <li><%= link_to t(:logout), destroy_user_session_path, method: :delete %></li>
                      <% else %>
                    <li><%= link_to t(:login), new_user_session_path %></li>
                      <% end %>
            		</ul>
                </div>
        </div>
    </div>
</div>